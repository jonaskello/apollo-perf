{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "start": "run-p -l start-client start-server",
    "start-client": "cd packages/apollo-perf-client && yarn start",
    "start-server": "cd packages/apollo-perf-server && yarn start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.2"
  }
}
